import styled from 'styled-components';

export const ContentContainer = styled.div`
  display: grid;
  grid-auto-flow: column;
  gap: 32px;
  grid-template-columns: 1fr 1fr;
`

export const TitleCard = styled.h2`
  margin: 0;
  padding: 2px;
`

export const InfoCards = styled.p`
  margin: 0;
  padding: 4px;
`